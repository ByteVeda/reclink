# Docusaurus assumes a flat node_modules. 3.10.2 no longer duplicates
# @docusaurus/theme-common, so the build itself survives pnpm's isolated layout,
# but @types/react only arrives transitively and tsc cannot resolve it from
# there — `pnpm typecheck` fails with TS7016 on every React import.
node-linker=hoisted
