# Docusaurus assumes a flat node_modules. Under pnpm's default isolated layout
# @docusaurus/theme-common gets installed twice (different peer sets), so
# theme-mermaid reads a different React context than the one Layout provides and
# the build fails with "useColorMode is called outside the <ColorModeProvider>".
node-linker=hoisted
