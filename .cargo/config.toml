# getrandom 0.3+ refuses to build for wasm32-unknown-unknown unless the backend is
# selected explicitly; the `wasm_js` feature alone is not enough. Scoped to the wasm
# target so native builds are unaffected.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
